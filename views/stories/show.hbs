<div class="row">
    <div class="col s12 m8">
      <h3>{{story.title}}
             <!-- <small>{{{editIcon story.user user story._id false}}}</small>
        </h3>-->

        <div class="card story">
            <div class="card-content">
                <span class="card-title">{{formatDate date 'MMMM Do YYYY, h:mm:ss a'}}</span>
              
                <span class="card-title"><p>category :</p>{{story.category}}</span>
                {{{story.body}}}
            </div>
        </div>
    </div>
   <!--  <div class="col s12 m4">
        <div class="card center-align">
            <div class="card-content">-->
             <!--   <span class="card-title">{{story.user.displayName}}</span>
                <img src="{{story.user.image}}" class="circle responsive-img img-small">
            </div>
            <div class="card-action">-->
                <!--<a href="/stories/user/{{story.emailUser._id}}">More From {{story.emailUser.name}}</a>-->
                
            <!--{{#if story.emailUser}}
             <a href="/stories/user/{{story.emailUser}}">More From {{story.emailUser.name}}</a>
            {{else}}
         <a href="/stories/user/{{story.googleUser._id}}">More From {{story.googleUser.firstname}}</a>
            {{/if}}-->
            
            </div>
        </div>
    </div>
</div>